# Core FastAPI and web framework (Vercel compatible)
fastapi==0.104.1
uvicorn==0.23.2
python-multipart==0.0.6
python-dotenv==1.0.0
pydantic==2.4.2
jinja2==3.1.2

# Essential web processing
requests==2.31.0
beautifulsoup4==4.12.2

# Minimal dependencies for basic functionality
httpx==0.25.0

# Note: Removed for Vercel serverless compatibility:
# - pymupdf (requires system dependencies and compilation)
# - sentence-transformers (too heavy, requires torch)
# - chromadb (requires compilation and system dependencies)
# - transformers (too large for serverless)
# - langchain packages (depend on heavy ML libraries)
# - huggingface-hub (depends on transformers)
#
# This minimal version provides basic web functionality.
# For full AI features, consider using external APIs or
# deploying to a platform that supports heavier dependencies.
